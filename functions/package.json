{
  "name": "functions",
  "description": "Cloud Functions for ISP Education — payouts & admin utilities",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "eslint --ext .js . || true",
    "deploy": "firebase deploy --only functions",
    "start": "firebase emulators:start --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.6",
    "axios": "^1.13.2",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.0",
    "node-fetch": "^2.6.7",
    "twilio": "^5.10.6"
  },
  "devDependencies": {
    "eslint": "^8.48.0"
  }
}